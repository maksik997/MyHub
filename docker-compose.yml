services:
  db:
    image: postgres:16
    container_name: postgres-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: myhub
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    env_file:
      - ./.env
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - ${DB_DATA}:/var/lib/postgresql/data

  myhub-app:
    build: .
    container_name: myhub-app
    restart: unless-stopped
    ports:
      - "${PORT}:8080"
    volumes:
      - ${MYHUB_GAME_DIRECTORY}:/app/games
      - ${MYHUB_MEDIA_DIRECTORY}:/app/media
    environment:
      DB_URL: jdbc:postgresql://db:${DB_PORT}/myhub
    env_file:
      - ./.env
    depends_on:
      - db
